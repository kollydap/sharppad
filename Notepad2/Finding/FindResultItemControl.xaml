<UserControl x:Class="Notepad2.Finding.FindResultItemControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d" 
             MinHeight="50"
             MaxHeight="120"
             Background="Transparent"
             Foreground="{DynamicResource ControlDefaultForeground}">
    <Grid Background="Transparent" MouseDown="Grid_MouseDown">
        <TextBlock Text="Start Index:" VerticalAlignment="Top" HorizontalAlignment="Left" Width="60" Margin="0,2,0,0"/>
        <TextBlock Text="{Binding Result.StartIndex}"
                   TextTrimming="WordEllipsis" TextWrapping="NoWrap" 
                   Margin="0,18,0,2" HorizontalAlignment="Left" Width="60" TextAlignment="Center"/>
        <Grid Margin="62,2,0,2" HorizontalAlignment="Left" Width="128">
            <TextBlock Text="Preview of Text" VerticalAlignment="Top" HorizontalAlignment="Center"/>
            <TextBlock Text="{Binding Result.PreviewFoundText}" TextAlignment="Center" TextTrimming="CharacterEllipsis" TextWrapping="NoWrap" Visibility="Visible" Margin="0,16,0,0"/>
        </Grid>
    </Grid>
</UserControl>
