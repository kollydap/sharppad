<UserControl x:Class="Notepad2.History.HistoryControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Notepad2.History"
             mc:Ignorable="d" 
             Background="Transparent"
             MaxHeight="39" MinHeight="39" d:DesignWidth="200"
             MouseDoubleClick="UserControl_MouseDoubleClick" Foreground="{DynamicResource ControlDefaultForeground}">
    <Grid Background="Transparent">
        <TextBlock Text="Name:" VerticalAlignment="Top" Margin="0,2,0,0" HorizontalAlignment="Left"/>
        <TextBlock Text="{Binding Document.FileName, UpdateSourceTrigger=PropertyChanged}" VerticalAlignment="Top" Margin="37,2,52,0"/>
        <TextBlock Text="Path:" VerticalAlignment="Top" Margin="0,21,0,0" HorizontalAlignment="Left"/>
        <TextBlock Text="{Binding Document.FilePath, UpdateSourceTrigger=PropertyChanged}" VerticalAlignment="Top" Margin="37,21,52,0"/>
        <Button Content="Reopen" VerticalAlignment="Center" Height="33" HorizontalAlignment="Right" Margin="0" Width="50" Click="Button_Click"/>
    </Grid>
</UserControl>
